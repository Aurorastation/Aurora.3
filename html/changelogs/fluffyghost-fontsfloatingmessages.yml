################################
# Example Changelog File
#
# Note: This file, and files beginning with ".", and files that don't end in ".yml" will not be read. If you change this file, you will look really dumb.
#
# Your changelog will be merged with a master changelog. (New stuff added only, and only on the date entry for the day it was merged.)
# When it is, any changes listed below will disappear.
#
# Valid Prefixes:
#   bugfix
#   wip (For works in progress)
#   tweak
#   soundadd
#   sounddel
#   rscadd (general adding of nice things)
#   rscdel (general deleting of nice things)
#   imageadd
#   imagedel
#   maptweak
#   spellcheck (typo fixes)
#   experiment
#   balance
#   admin
#   backend
#   security
#   refactor
#################################

# Your name.
author: FluffyGhost

# Optional: Remove this file after generating master changelog.  Useful for PR changelogs that won't get used again.
delete-after: True

# Any changes you've made.  See valid prefix list above.
# INDENT WITH TWO SPACES.  NOT TABS.  SPACES.
# SCREW THIS UP AND IT WON'T WORK.
# Also, all entries are changed into a single [] after a master changelog generation. Just remove the brackets when you add new entries.
# Please surround your changes in  double quotes ("), as certain characters otherwise screws up compiling. The quotes will not show up in the changelog.
changes:
  - refactor: "Refactored floating messages."
  - bugfix: "Images that carry the floating messages for the chat are now sized according to the message size and generated per-client."
  - refactor: "Refactored overlays management through the procs, ported from TG."
  - backend: "Updated pickup animations to use the new system, minor refactor of it."
  - backend: "Flick overlay visual now uses a /atom/movable/flick_visual carrying object."
  - backend: "Ported various fonts and the /datum/font system."
  - rscadd: "Floating messages for the chat and actions now use a pixel-perfect font with selected sizes."
  - backend: "Reduced the number of spawn()s required to send messages."
  - bugfix: "Non-ASCII characters are now not mangled anymore in the runechat messages."
  - refactor: "Consolidated /atom/Destroy in a single proc and moved it in the appropriate file."
  - refactor: "Moved a bulk of /atom vars in the aforementioned appropriate file."
  - backend: "/atom/movable/flick_visual now tracks who it was added in the vis_contents of and removes itself from it on destroy, thus avoiding needing an harddel."
  - backend: "Added the runechat subsystem to try to compensate for tick overruns caused by waking up pf the rune proc."
  - backend: "Split the processing of the runechat icon to take advantage of the above."
