/proc/log_subsystem_mapfinalization(text)
	logger?.Log(LOG_CATEGORY_SUBSYSTEM_MAPFINALIZATION, "SSMapfinalization: [text]")

/**
 * This is used to highlight errors in github
 */
/proc/log_subsystem_mapfinalization_error(text)
	logger?.Log(LOG_CATEGORY_SUBSYSTEM_MAPFINALIZATION, "ERROR: SSMapfinalization: [text]")
