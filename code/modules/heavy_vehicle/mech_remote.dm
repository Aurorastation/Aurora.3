// A simple file that holds the global lists of remote controlled mechs

var/global/list/remotemechs = list() // A wildcard list
var/global/list/prisonmechs = list() // Mechs that prisoners can use to mine while in communal